{
    "version": 2,
    "builds": [
      { "src": "vercel.json", 
      "use": "vercel-sapper"}
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/main.py" }
    ],
    "env": {
      "APP_MODULE": "main:app"
    }
  }